The code can be build using the Apache ant build file:

ant dist

Then, if the task is completed successfully, the command to run an example (taken from the OAEI 2010 benchmark) is

java -Xmx4000m -jar dist/knofuss-standalone.jar -c example/example-config-restaurant.rdf
